# Milestone 3 — Algorithms & Experimental Plan

> **Status:** not started · depends on M2

---

## 1. Formal Problem Statement

### 1.1 Data Provenance (WhyProv)

*TBD — Input / Output / Objective.*

### 1.2 Query Provenance (QueryRewrite)

*TBD — Input / Output / Objective.*

---

## 2. Algorithms

### 2.1 WhyProv (pseudo-code)

```
ALGORITHM WhyProv(user u, movie m, int K, index I)
  TBD — line-numbered pseudo-code
```

### 2.2 QueryRewrite (pseudo-code)

```
ALGORITHM QueryRewrite(user u, movie m*, query Q)
  TBD — line-numbered pseudo-code
```

### 2.3 Baselines

- **Baseline A:** full-scan why-provenance (remove one tuple at a time, rerun CF).
- **Baseline B:** exhaustive query rewrite (enumerate all parameter combinations).

---

## 3. Experimental Plan

### 3.1 Setup

*TBD — CPU, RAM, Python version.*

### 3.2 Algorithms Compared

- Ours: `WhyProv`, `QueryRewrite` (index-based)
- Baselines: A, B (above)

### 3.3 Metrics

- **Runtime** (seconds per explanation)
- **Provenance size** (# tuples returned)
- **Top-K Jaccard preservation** (for QueryRewrite)
- **Index build time + storage overhead**

### 3.4 Factors

- K ∈ {5, 10, 20, 50}
- User activity (# ratings by u) ∈ {low, medium, high}
- Dataset subsample size ∈ {25%, 50%, 100%}

### 3.5 Expected Results

- Runtime speedup ≥ 5×
- Provenance size reduction ≥ 50%
- Top-K preservation ≥ 0.7
- Index build time < 1 s, storage overhead < 2× raw data
